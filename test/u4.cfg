# Unit test configuration file for MessageLogger service:
# statistics destination :  name not matching any destination; 
# no PSet configuring it specially

process TEST = 
{
  service = MessageLogger 
  {
    
    vstring destinations = { "u4_errors.mlog"
			   }

    vstring statistics = { 
    			    "u4_statistics.mlog"
			  , "anotherStats" 
			  , "u4_errors.mlog" 
			 }

    vstring categories = { "preEventProcessing" }
			      
    PSet u4_errors.mlog   = { bool noTimeStamps = true  
                              string threshold  = "ERROR"    
			      PSet preEventProcessing = { int32 limit    = 0 }
			    }  
			    
    PSet anotherStats        = { string output = "u4_another.mlog" }
    PSet u4_statistics.mlog  = { string threshold  = "WARNING"   }
  }
  
  path p = { sendSomeMessages }

  module sendSomeMessages = UnitTestClient_A { }

  source = EmptySource {untracked int32 maxEvents = 3}
}
