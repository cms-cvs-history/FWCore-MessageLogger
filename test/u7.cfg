# Unit test configuration file for MessageLogger service:
#   Filtering all but one category (for example FwkJob)

process TEST = 
{
  service = MessageLogger 
  {
    
    untracked
    vstring destinations = { "u7_log.mlog"
    			   , "u7_restrict.mlog" 
			   }

    untracked
    vstring fwkJobReports = { "u7_job_report.mxml" }

    untracked
    vstring categories = { "FwkJob", "special" }

    untracked
    PSet u7_log.mlog        = { untracked bool noTimeStamps = true      
			      }
			      
    untracked
    PSet u7_restrict.mlog   = { untracked bool noTimeStamps = true  
                                untracked PSet default  =  {  untracked int32 limit    = 0 }
				untracked PSet special  =  {  untracked int32 limit    = -1 }
			      }
			      
    untracked
    PSet u7_job_report.mxml = { untracked bool noTimeStamps = true  
			      }
			      
  }
  
  path p = { sendSomeMessages }

  module sendSomeMessages = UnitTestClient_D { }

  source = EmptySource {untracked int32 maxEvents = 2}
}
