# Unit test configuration file for MessageLogger service:
# hex and other manipulators

process TEST = 
{
  service = MessageLogger 
  {
    
    untracked
    vstring destinations = { "u6_warnings.mlog" }

			      
    PSet u6_warnings.mlog = { bool noTimeStamps = true  
                              string threshold  = "WARNING" 
			    }
			      
  }
  
  path p = { sendSomeMessages }

  module sendSomeMessages = UnitTestClient_C { }

  source = EmptySource {untracked int32 maxEvents = 1}
}
